<%= bootstrap_form_for(@rubrique, inline_errors: false) do |f| %>
  <% if @rubrique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rubrique.errors.count, "error") %> prohibited this rubrique from being saved:</h2>

      <ul>
      <% @rubrique.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= f.collection_select(:cour_id, Cour.all, :id, :titre) %>
    </div>
  <div class="field">
    <%= f.text_field :titre %>
  </div>
  <div class="field">
    <%= f.text_area :contenu %>
  </div>
    <div class="field">
      <%= f.text_area :objectif %>
    </div>
  <div class="actions">
    <%= f.submit 'Valider' %>
  </div>
<% end %>
